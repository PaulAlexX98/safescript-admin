import ClassicEditor from '@ckeditor/ckeditor5-build-classic';

// If you want extra plugins (tables/colors/fullscreen) not in the default build,
// we can swap to a custom build or extend via plugins here.
window.ClassicEditor = ClassicEditor;